import { Navbar } from '../../components';
import './index .scss';

const Landing = () => {
    return (
        <section className="landing">
            <Navbar />
        </section>
    );
};

export default Landing;