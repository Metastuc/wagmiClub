import './index.scss';

const Logo = () => {
    return (
        <section className="logo">
            <div className="logo-wrapper">
                <span>wagmi</span>
                <span>club</span>
            </div>
        </section>
    );
};

export default Logo;