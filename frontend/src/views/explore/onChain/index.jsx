import React from "react";
import "./index.scss";

const OnChain = ({ group }) => {
	return (
		<section className={`${group}`}>
			<div className={`{group}__wrapper`}>
                <>tabs</>
            </div>
		</section>
	);
};

export default OnChain;
